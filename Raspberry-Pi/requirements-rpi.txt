# STEMSight PIM - Raspberry Pi 4 Auto Broadcaster Dependencies
# Optimized for ARM64 architecture and automated streaming

# Core Python packages
numpy>=1.21.0
Pillow>=9.0.0

# WebRTC & Streaming (Primary Dependencies)
aiortc>=1.4.0
aiohttp>=3.8.0
av>=10.0.0

# Camera & Hardware
picamera2>=0.3.8
libcamera>=0.0.1
RPi.GPIO>=0.7.1

# Computer Vision & AI (for UNIK integration)
torch>=1.12.0
torchvision>=0.13.0
mediapipe>=0.8.10
opencv-python>=4.6.0

# Networking & Communication
requests>=2.28.0
websockets>=10.3

# Configuration & Utilities
pyyaml>=6.0
python-dotenv>=0.20.0
psutil>=5.9.0

# Logging & Monitoring
structlog>=22.1.0

# System monitoring
gpiozero>=1.6.2

# Development & Testing (optional)
pytest>=7.1.0
pytest-asyncio>=0.19.0

# Installation Notes:
# 1. For PyTorch on Raspberry Pi: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# 2. Ensure camera is enabled: sudo raspi-config -> Interface Options -> Camera -> Enable
# 3. For best performance: sudo raspi-config -> Advanced Options -> Memory Split -> 128